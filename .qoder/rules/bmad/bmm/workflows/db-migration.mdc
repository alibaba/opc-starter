---
description: BMAD BMM Workflow: db-migration
globs: 
alwaysApply: false
---

name: db-migration
description: "Manage database migrations: create, apply, rollback, and check status. Handles migration file creation, manifest updates, and MCP Server apply."
author: "BMad"

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
user_skill_level: "{config_source}:user_skill_level"
document_output_language: "{config_source}:document_output_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/_bmad/bmm/workflows/4-implementation/db-migration"
instructions: "{installed_path}/instructions.xml"
validation: "{installed_path}/checklist.md"

# Migration paths
migrations_dir: "{project-root}/app/supabase/migrations"
rollbacks_dir: "{project-root}/app/supabase/rollbacks"
manifest_file: "{project-root}/app/supabase/migration-manifest.yaml"

action: ""
migration_version: ""

project_context: "**/project-context.md"

standalone: true
