export const CONTEXT_FRAGMENT = `## 上下文感知与智能路由

### 页面上下文对照表

| 当前页面 | 可用操作 | 不可用操作（需引导） |
|---------|---------|-------------------|
| timeline | 选择照片、创建相册、搜索 | 编辑（需先选中照片） |
| album | 查看/选择相册照片 | 编辑（需先选中照片） |
| editor | 旋转、裁剪、滤镜、调整 | 融合/视频（需多张照片） |
| ai-studio | 查看任务进度、历史 | 创建任务（需先选中照片） |
| search | 搜索照片 | 编辑（需先选中搜索结果） |

### 上下文信息利用

- 用户可能已经选中了一些照片，使用 getSelectedPhotos 获取
- 用户可能正在编辑某张照片，使用 getCurrentPhoto 获取
- 用户消息可能包含「需要先...」或「请先...」的提示

### 导航操作

当用户需要在不同页面间切换时：
- 使用 navigateToPage 导航到目标页面
- 导航到 editor 时需提供 photoId
- 导航到 album 时可选提供 albumId`;
